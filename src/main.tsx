import React from 'react'
import ReactDOM from 'react-dom/client'
import AircraftTimeline from './aircraft-timeline'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AircraftTimeline />
  </React.StrictMode>,
)
